<ui:FluentWindow x:Class="ViveGui.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:vm="clr-namespace:ViveGui.ViewModels"
                 xmlns:conv="clr-namespace:ViveGui.Converters"
                 d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
                 mc:Ignorable="d"
                 Title="ViVeTool GUI Wrapper" 
                 Height="650" Width="850"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica">

    <Window.Resources>
        <conv:StatusToBrushConverter x:Key="StatusToBrushConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Custom TitleBar -->
            <RowDefinition Height="Auto"/>
            <!-- Input Area -->
            <RowDefinition Height="*"/>
            <!-- Data Grid -->
            <RowDefinition Height="Auto"/>
            <!-- Action Area -->
        </Grid.RowDefinitions>

        <!-- 0. 自定义标题栏 (WinUI 风格必须) -->
        <ui:TitleBar Grid.Row="0" Title="ViVeTool GUI Wrapper" />

        <!-- 1. Input Area -->
        <Grid Grid.Row="1" Margin="20,10,20,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ComboBox Grid.Column="0" 
                      ItemsSource="{Binding ActionTypes}" 
                      SelectedItem="{Binding SelectedAction}"
                      Width="120" Margin="0,0,10,0" 
                      VerticalAlignment="Center"/>

            <!-- 使用 PlaceholderText 代替 ToolTip，更符合现代 UI -->
            <ui:TextBox Grid.Column="1" 
                        Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                        PlaceholderText="Enter IDs here (e.g. '12345 67890+1')"
                        VerticalAlignment="Center" 
                        Icon="{ui:SymbolIcon Edit16}"/>

            <!-- 普通操作按钮 -->
            <ui:Button Grid.Column="2" 
                       Command="{Binding AddCommand}" 
                       Content="Add to Queue"
                       Icon="{ui:SymbolIcon Add24}"
                       Margin="10,0,0,0" />
        </Grid>

        <!-- 2. Data Grid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Instructions}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  RowDetailsVisibilityMode="Visible"
                  Margin="20,0,20,0"
                  BorderThickness="1"
                  BorderBrush="{DynamicResource ControlElevationBorderBrush}">

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Background" Value="{Binding Status, Converter={StaticResource StatusToBrushConverter}}"/>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID &amp; Variant" Binding="{Binding GetIdString}" Width="2*"/>

                <DataGridTemplateColumn Header="Action" Width="1*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <!-- 使用卡片式标签展示 Action -->
                            <Border Background="{DynamicResource ControlSolidFillColorDefaultBrush}" 
                                    CornerRadius="4" Padding="8,2" HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Action}"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="1.5*"/>

                <!-- 操作列: 使用图标按钮 -->
                <DataGridTemplateColumn Header="Ops" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button Command="{Binding DataContext.RemoveRowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                       CommandParameter="{Binding}" 
                                       Appearance="Secondary"
                                       Icon="{ui:SymbolIcon Delete24}"
                                       Padding="5"
                                       ToolTip="Remove"
                                       BorderThickness="0" Background="Transparent"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <!-- 使用 CardControl 或者带有圆角的 Border -->
                    <Border Margin="10,5,10,10" 
                            Background="{DynamicResource LayerOnMicaBaseAltFillColorDefaultBrush}" 
                            CornerRadius="4" 
                            Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <ui:SymbolIcon Symbol="Code24" Margin="0,0,10,0" VerticalAlignment="Top" Foreground="Gray"/>

                            <TextBox Grid.Column="1"
                                     Text="{Binding OutputText, Mode=OneWay}" 
                                     IsReadOnly="True" 
                                     TextWrapping="Wrap" 
                                     FontFamily="Consolas"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     MaxHeight="150"
                                     VerticalScrollBarVisibility="Auto"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
        </DataGrid>

        <!-- 3. Action Area -->
        <Border Grid.Row="3" 
                Background="{DynamicResource SolidBackgroundFillColorBaseBrush}" 
                BorderThickness="0,1,0,0" 
                BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}"
                Padding="20,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                <!-- 清除按钮：次要操作 -->
                <ui:Button Content="Clear All" 
                           Command="{Binding ClearCommand}" 
                           Icon="{ui:SymbolIcon Dismiss24}"
                           Appearance="Secondary"
                           Margin="0,0,10,0" MinWidth="100"/>

                <!-- 执行按钮：主要操作 (高亮显示) -->
                <ui:Button Content="Execute Batch" 
                           Command="{Binding RunCommand}" 
                           Icon="{ui:SymbolIcon Play24}"
                           Appearance="Primary"
                           MinWidth="120"/>
            </StackPanel>
        </Border>

        <ContentPresenter x:Name="RootContentDialog" Grid.Row="0" Grid.RowSpan="4" />
    </Grid>
</ui:FluentWindow>